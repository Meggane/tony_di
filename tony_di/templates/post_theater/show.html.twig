{% extends 'base.html.twig' %}

{% block title %}Tony Di - Théâtre{% endblock %}

{% block body %}
    <h1>Théâtre</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ post_theater.id }}</td>
        </tr>
        <tr>
            <th>Title</th>
            <td>{{ post_theater.title }}</td>
        </tr>
        <tr>
            <th>Image</th>
            <td>{{ post_theater.image }}</td>
        </tr>
        <tr>
            <th>Content</th>
            <td>{{ post_theater.content }}</td>
        </tr>
        <tr>
            <th>Link_teaser</th>
            <td>{{ post_theater.linkTeaser }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('post_theater_index') }}">Retour à la liste</a>

    <a href="{{ path('post_theater_edit', {'id': post_theater.id}) }}">Modifier</a>

    {{ include('post_theater/_delete_form.html.twig') }}
{% endblock %}
